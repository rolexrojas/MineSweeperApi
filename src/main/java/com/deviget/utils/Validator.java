package com.deviget.utils;

import com.deviget.types.SignInDTO;
import com.deviget.types.SignUpDTO;

public class Validator {
    public static boolean isValidSignUp(SignUpDTO signUpDTO){
        //TODO: ADD SOME USER VAR VALIDATIONS (LENGTH, COMPLETIXY ETC)
        return true;
    }

    public static boolean isValidSignIn(SignInDTO signInDTO){
        //TODO: ADD SOME USER VAR VALIDATIONS (LENGTH, COMPLETIXY ETC)
        return true;
    }
}
